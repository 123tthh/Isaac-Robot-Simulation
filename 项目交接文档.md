# Isaac OCS 项目交接文档

> **交接日期**: 2026-07-31  
> **项目路径**: 当前文件所在的项目根目录
> **文档版本**: v1.0

---

## 📋 目录

1. [项目概述](#1-项目概述)
2. [项目目标与完成情况](#2-项目目标与完成情况)
3. [技术架构](#3-技术架构)
4. [环境依赖](#4-环境依赖)
5. [目录结构](#5-目录结构)
6. [核心脚本与接口](#6-核心脚本与接口)
7. [启动流程](#7-启动流程)
8. [数据流程](#8-数据流程)
9. [Docker 使用](#9-docker-使用)
10. [物理仿真参数](#10-物理仿真参数)
11. [常见问题](#11-常见问题)
12. [重要备注](#12-重要备注)
13. [后续工作建议](#13-后续工作建议)
14. [参考文档](#14-参考文档)

---

## 1. 项目概述

### 1.1 项目简介

本项目是基于 **NVIDIA Isaac Sim 5.1.0** 的 R1 轮式双臂机器人仿真控制系统，集成了：
- **双臂控制**: 基于 OCS2 MPC（模型预测控制）的 7 自由度双臂控制
- **夹爪控制**: PGIA v4.5 协议的左右夹爪
- **底盘控制**: 差速驱动底盘
- **视觉系统**: 三组 RGB-D 相机（头部、左手、右手）
- **数据采集**: 人工示教、轨迹记录、LeRobot 格式转换

### 1.2 项目状态

**当前状态**: ✅ 基础功能已验收通过，六路相机修复完成，Docker 环境已验证

| 验证项 | 状态 | 备注 |
|--------|------|------|
| 本地 Isaac Sim 5.1.0 启动 | ✅ 通过 | 场景自动加载，六相机自动重建 |
| 双臂 OCS2 控制 | ✅ 通过 | 左右臂末端位姿控制正常 |
| 夹爪控制 | ✅ 通过 | PGIA v4.5 协议，开合正常 |
| 底盘控制 | ✅ 通过 | 差速驱动，实际位移验证通过 |
| 六路相机发布 | ✅ 通过 | RGB+Depth 全部正常发布 |
| Docker 完整构建 | ✅ 通过 | 116/116 包构建成功 |
| Docker 运行验证 | ✅ 通过 | OCS2、夹爪、底盘、相机全部验证 |
| 示教与回放 | ✅ 通过 | 轨迹记录、清洗、回放正常 |
| LeRobot 数据转换 | ✅ 功能完备 | v3.0 格式转换脚本可用 |
| 自动随机化与批量训练 | ⏳ 未完成 | 需后续开发 |

---

## 2. 项目目标与完成情况

### 2.1 已完成目标

✅ **仿真环境搭建**
- Isaac Sim 5.1.0 场景创建与配置
- R1 双臂机器人 URDF/USD 集成
- Action Graph 控制链路搭建

✅ **控制系统开发**
- OCS2 双臂 MPC 控制器
- ros2_control 与 topic_based_ros2_control 桥接
- 夹爪 PGIA v4.5 协议实现
- 差速底盘控制

✅ **视觉系统**
- 三组 RGB-D 相机（6 路数据流）
- RenderProduct 会话级自动重建
- 相机数据录制与保存

✅ **数据采集与处理**
- 键盘示教系统（pygame 多键控制）
- OCS2 轨迹记录与回放
- 底盘路径记录与回放
- 轨迹清洗与诊断
- LeRobot v3.0 格式转换

✅ **Docker 化部署**
- 完整 Dockerfile（基于 Isaac Sim 5.1.0 官方镜像）
- 离线依赖包（wheelhouse）
- 116 个 ROS 2 包构建验证
- 运行环境验证

✅ **文档与可复现性**
- 启动指南、操作手册
- Docker 构建文档
- 依赖锁定清单
- 备份与校验机制

### 2.2 未完成目标

⏳ **自动化数据采集**
- 场景自动随机化
- 任务成功自动判定
- 批量数据扩增

⏳ **模型训练**
- BC/ACT/Diffusion 训练管线
- VLA 集成
- RL 训练环境

⏳ **Sim2Real**
- 真机标定
- 域适应
- 安全验收

### 2.3 当前数据集状态

**已有数据**:
- 保留一组 2026-07-15 原始示教轨迹
- 清洗后的 OCS2 轨迹数据
- Docker 验收用 5 秒六路相机数据

**数据不足**:
- ❌ 无完整的任务标注数据集
- ❌ 无足够的视觉训练样本
- ❌ 无批量采集的多样化数据

---

## 3. 技术架构

### 3.1 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    Isaac Sim 5.1.0                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ R1 Robot USD │  │ Action Graphs│  │6x RenderProd │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└─────────────────────────────────────────────────────────────┘
                           │ ROS 2 Bridge
┌─────────────────────────────────────────────────────────────┐
│                      ROS 2 (Humble/Jazzy)                   │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  OCS2 MPC Controller  │  ros2_control  │  TF/URDF   │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                           │
┌─────────────────────────────────────────────────────────────┐
│                    SIM1 Control Layer                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐        │
│  │  Keyboard   │  │  Trajectory │  │   Camera    │        │
│  │   Teleop    │  │   Replay    │  │   Recorder  │        │
│  └─────────────┘  └─────────────┘  └─────────────┘        │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 控制链路

#### 双臂控制流程
```
键盘输入/CSV轨迹
  ↓
/left_target/stamped, /right_target/stamped (PoseStamped)
  ↓
ocs2_arm_controller (OCS2 MPC)
  ↓
/arm_joint_cmd (Float64MultiArray)
  ↓
topic_based_ros2_control (桥接)
  ↓
Isaac Sim Arm_Control_Graph
  ↓
双臂关节执行
```

#### 夹爪控制流程
```
Space/V/B/N/M 键
  ↓
/left_gripper_controller/commands (Float64MultiArray)
/right_gripper_controller/commands (Float64MultiArray)
  ↓
Isaac Sim Gripper_Control_Graph
  ↓
script_gripper_v4.5.py (PGIA 协议)
  ↓
positionCommand → 夹爪关节
```

**PGIA v4.5 命令语义**:
- `cmd > 0`: 打开夹爪
- `cmd <= 0`: 闭合/保持夹爪

#### 底盘控制流程
```
方向键 (↑↓←→)
  ↓
/sim1/base_diff_cmd (Twist)
  ↓
Isaac Sim Base_Drive_Graph
  ↓
左右主动轮
```

#### 相机发布流程
```
Isaac Sim RenderProduct (会话级对象)
  ↓
Camera_Publish_Graph (6x CameraHelper)
  ↓
ROS 2 Image Topics:
  - /head_cam/color/image_raw
  - /head_cam/depth/image_rect_raw
  - /left_cam/color/image_raw
  - /left_cam/depth/image_rect_raw
  - /right_cam/color/image_raw
  - /right_cam/depth/image_rect_raw
```

---

## 4. 环境依赖

### 4.1 本地环境

| 组件 | 版本/路径 | 说明 |
|------|-----------|------|
| 操作系统 | Ubuntu 22.04 LTS | Linux 6.8.0-111-generic |
| GPU | NVIDIA GeForce RTX 5090 D v2 | - |
| 驱动 | 580.126.09 | nvidia-smi 可查看 |
| Isaac Sim | 5.1.0-rc.19 | `/home/gtk/isaac-sim-5.1` |
| ROS 2 | Humble | `/opt/ros/humble` |
| Python | 3.10 (系统), 3.11 (Isaac) | Isaac 使用自带 Python |
| colcon | 已安装 | ROS 2 构建工具 |
| Docker | 已安装 | 用于容器化部署 |

**重要环境变量**:
```bash
export ISAAC_SIM_ROOT=/home/gtk/isaac-sim-5.1
export ROS_DOMAIN_ID=73  # 推荐使用，避免冲突
source /opt/ros/humble/setup.bash
source projects/ros2_ws/install/setup.bash
```

### 4.2 Docker 环境

| 组件 | 版本 | 说明 |
|------|------|------|
| 基础镜像 | `nvcr.io/nvidia/isaac-sim:5.1.0` | SHA256 已固定 |
| 当前镜像 | `issac_ocs_docker:5.1.0-repro` | ID: 7922d3590bde |
| ROS 2 | Jazzy | Ubuntu 24.04 |
| Gazebo | Harmonic | `GZ_VERSION=harmonic` |
| RMW | rmw_fastrtps_cpp | Fast DDS |
| Python | 3.11.13 | Isaac 自带 |
| ONNX Runtime | 1.19.0 C++ SDK | `/opt/onnxruntime` |

**Docker 构建包数量**: 116/116 (排除 3 个 Raisim 包)

**排除的包** (需要额外 SDK 和许可证):
- `ocs2_raisim_core`
- `ocs2_legged_robot_raisim`
- `ocs2_legged_robot_mpcnet`

### 4.3 依赖清单文件

| 文件 | 作用 |
|------|------|
| `dependencies/docker-apt-packages.lock` | apt 包列表 |
| `dependencies/docker-python-requirements.lock` | Python 包锁定 |
| `dependencies/docker-wheelhouse/` | 离线 wheel 文件 (150+ 个) |
| `dependencies/docker-wheelhouse.sha256` | wheel 文件校验 |
| `dependencies/docker_source_cache/` | ONNX Runtime 等源码包 |
| `dependencies/repositories.lock.yaml` | 外部 Git 仓库版本锁定 |
| `vendor/isaac_sim_5_1_python/` | Isaac Python 补充包 |


---

## 5. 目录结构

### 5.1 项目根目录

```
./
├── assets/                          # 场景与资源
│   ├── scenes/
│   │   ├── scene.usd               # ⭐ 唯一启动场景入口
│   │   ├── 615scene_20260723/      # 场景依赖层（不可删除）
│   │   └── 513_physics.usd         # 物理参数场景
│   └── datasets/                    # 数据集目录
│
├── projects/                        # 核心工程
│   ├── ros2_ws/                    # ⭐ ROS 2 工作区
│   │   ├── src/                    # 源码
│   │   ├── build/                  # 本地构建（宿主机）
│   │   ├── install/                # 本地安装（宿主机）
│   │   ├── log/                    # 本地日志（宿主机）
│   │   ├── build_docker/           # Docker 构建
│   │   ├── install_docker/         # Docker 安装
│   │   └── log_docker/             # Docker 日志
│   │
│   ├── Trajectory/SIM1/            # ⭐ 示教与数据采集
│   │   ├── game_control.sh         # SIM1 统一入口
│   │   ├── keyboard_ocs2_gripper_teleop.py  # 示教脚本
│   │   ├── replay_ocs2_target_trace.py      # 轨迹回放
│   │   ├── trace_data/             # 轨迹数据
│   │   │   ├── raw/                # 原始数据
│   │   │   ├── cleaned/            # 清洗后数据
│   │   │   └── lerobot_v30/        # LeRobot 格式
│   │   └── tools/                  # 工具脚本
│   │       ├── camera_grid_viewer.py    # 六相机监看
│   │       └── camera_recorder.py       # 相机录制
│   │
│   └── IsaacLab-Arena/             # IsaacLab (可选)
│
├── scripts/                         # ⭐ 项目脚本
│   ├── project_control_20260723.sh          # 宿主机统一入口
│   ├── verify_project_layout.sh             # 项目验证
│   ├── docker_build_ros2_ws_20260730.sh     # Docker 构建脚本
│   ├── rebuild_six_camera_render_products_20260730.py  # 相机重建
│   └── inspect_usd_scene_20260723.py        # USD 场景检查
│
├── docker/                          # Docker 配置
│   ├── Dockerfile                   # 镜像构建文件
│   ├── docker-compose.yml           # Compose 配置
│   ├── run_isaac_ocs_docker.sh      # 容器启动脚本
│   ├── entrypoint.sh                # 容器入口
│   └── shell_aliases.sh             # 别名定义
│
├── dependencies/                    # 依赖管理
│   ├── docker-wheelhouse/           # 离线 Python 包
│   ├── docker_source_cache/         # 源码包缓存
│   └── repositories.lock.yaml       # 外部仓库锁定
│
├── data/                            # 数据目录
│   └── ros2_log/                    # ROS 日志与诊断
│
├── outputs/                         # 输出目录
│   ├── camera_session_rebuild/      # 相机重建结果
│   └── docker_acceptance_20260730/  # Docker 验收数据
│
├── backups/                         # 备份目录
│   ├── camera_session_fix_20260730/ # 相机修复备份
│   └── docker_acceptance_20260730/  # Docker 验收备份
│
├── reproducibility/                 # 可复现性材料
│   ├── artifacts/                   # 制品（大文件）
│   ├── manifests/                   # 校验清单
│   └── patches/                     # 补丁文件
│
├── 物理仿真与参数获取/              # 物理参数搜索
│   ├── SEARCH8/                     # 最新搜索脚本
│   └── result/                      # 搜索结果
│       └── final_release_space.json # 最终参数空间
│
├── docs/                            # 文档
│   ├── PROJECT_HANDOVER_20260730_zh.md      # 原交接文档
│   ├── startup_guide_20260723_zh.md         # 启动指南
│   └── ROBOT_RESOURCE_POLICY.md             # 资源策略
│
├── reports/                         # 报告
├── spec/                            # 规范
├── log/                             # colcon 日志
├── project_manifest.yaml            # ⭐ 项目配置清单
├── README_CN.md                     # 中文说明
├── GAME_GUIDE.md                    # 操作指南
└── REPRODUCE.md                     # 复现指南
```

### 5.2 核心 ROS 2 包

```
projects/ros2_ws/src/
├── r1_description/              # R1 机器人描述（URDF/Xacro）
├── ocs2_arm_controller/         # 双臂 OCS2 MPC 控制器
├── topic_based_ros2_control/    # Topic 与 ros2_control 桥接
├── arms_target_manager/         # RViz 交互目标管理
├── r1_lerobot_sim/              # LeRobot 集成与回放
├── ocs2_ros2/                   # OCS2 核心库
│   ├── core/
│   ├── mpc/
│   └── robotics/
├── arms_ros2_control/           # 机械臂 ros2_control
├── arms_moveit/                 # MoveIt 配置（可选）
└── ...（其他依赖包）
```

---

## 6. 核心脚本与接口

### 6.1 主要脚本列表

#### 宿主机启动脚本

| 脚本 | 功能 | 用法示例 |
|------|------|----------|
| `scripts/project_control_20260723.sh` | 统一启动入口 | `./scripts/project_control_20260723.sh start-isaac` |
| `scripts/verify_project_layout.sh` | 项目结构验证 | `./scripts/verify_project_layout.sh` |
| `scripts/rebuild_six_camera_render_products_20260730.py` | 相机重建 | 自动调用 |
| `scripts/inspect_usd_scene_20260723.py` | USD 场景检查 | `./scripts/project_control_20260723.sh inspect-usd` |

#### SIM1 脚本

| 脚本 | 功能 | 路径 |
|------|------|------|
| `game_control.sh` | SIM1 统一入口 | `projects/Trajectory/SIM1/` |
| `keyboard_ocs2_gripper_teleop.py` | 键盘示教 | `projects/Trajectory/SIM1/` |
| `replay_ocs2_target_trace.py` | 轨迹回放 | `projects/Trajectory/SIM1/` |
| `replay_lerobot_episode.py` | LeRobot 回放 | `projects/Trajectory/SIM1/` |
| `base_drive_recorder.py` | 底盘录制 | `projects/Trajectory/SIM1/` |
| `base_drive_path_runner.py` | 底盘路径回放 | `projects/Trajectory/SIM1/` |
| `camera_grid_viewer.py` | 六相机监看 | `projects/Trajectory/SIM1/tools/` |
| `camera_recorder.py` | 相机录制 | `projects/Trajectory/SIM1/tools/` |

#### Docker 脚本

| 脚本 | 功能 |
|------|------|
| `docker/run_isaac_ocs_docker.sh` | 容器启动 |
| `docker/entrypoint.sh` | 容器入口点 |
| `docker/shell_aliases.sh` | 容器内别名 |
| `scripts/docker_build_ros2_ws_20260730.sh` | ROS 工作区构建 |
| `scripts/docker_env_check.sh` | 环境检查 |

### 6.2 统一入口命令

#### `project_control_20260723.sh` 命令列表

```bash
# 环境检查
./scripts/project_control_20260723.sh preflight      # 预检（不启动）

# 构建
./scripts/project_control_20260723.sh build          # 构建核心包
./scripts/project_control_20260723.sh build --all    # 构建全部包

# 启动
./scripts/project_control_20260723.sh start-isaac    # 启动 Isaac Sim
./scripts/project_control_20260723.sh launch-ocs2    # 启动 OCS2

# SIM1
./scripts/project_control_20260723.sh sim1-check     # 检查运行状态
./scripts/project_control_20260723.sh sim1-reset     # 复位仿真
./scripts/project_control_20260723.sh sim1-teach     # 示教
./scripts/project_control_20260723.sh sim1-demo      # 回放

# 工具
./scripts/project_control_20260723.sh inspect-usd    # 检查 USD 场景
```

#### `game_control.sh` 命令列表

```bash
cd projects/Trajectory/SIM1

./game_control.sh check              # 检查 ROS 状态
./game_control.sh reset              # 复位
./game_control.sh teach              # 基础示教
./game_control.sh teach-with-cameras # 带相机示教
./game_control.sh demo               # 回放最新轨迹
./game_control.sh camera-grid        # 打开六相机监看
./game_control.sh base-record        # 底盘录制
./game_control.sh base-path-demo     # 底盘路径演示
./game_control.sh direct-demo        # LeRobot 直接回放
```

### 6.3 主要 ROS 2 接口

#### ROS 2 Topic 接口表

| Topic | 消息类型 | 方向 | 说明 |
|-------|---------|------|------|
| `/left_target/stamped` | `geometry_msgs/PoseStamped` | → | 左臂目标位姿 |
| `/right_target/stamped` | `geometry_msgs/PoseStamped` | → | 右臂目标位姿 |
| `/left_current_pose` | `geometry_msgs/PoseStamped` | ← | 左臂当前位姿 |
| `/right_current_pose` | `geometry_msgs/PoseStamped` | ← | 右臂当前位姿 |
| `/arm_joint_cmd` | `std_msgs/Float64MultiArray` | → | 双臂关节命令（14 轴）|
| `/isaac_joint_states` | `sensor_msgs/JointState` | ← | Isaac 原始关节状态 |
| `/joint_states` | `sensor_msgs/JointState` | ← | 补齐后关节状态 |
| `/left_gripper_controller/commands` | `std_msgs/Float64MultiArray` | → | 左夹爪命令 |
| `/right_gripper_controller/commands` | `std_msgs/Float64MultiArray` | → | 右夹爪命令 |
| `/sim1/base_diff_cmd` | `geometry_msgs/Twist` | → | 底盘差速命令 |
| `/fsm_command` | `std_msgs/Int32` | → | FSM 状态命令（1=HOME, 3=OCS2）|
| `/clock` | `rosgraph_msgs/Clock` | ← | 仿真时钟 |

#### 相机 Topic 接口表

| Topic | 消息类型 | 分辨率 | 频率 | frame_id |
|-------|---------|--------|------|----------|
| `/head_cam/color/image_raw` | `sensor_msgs/Image` | 640×480 | ~30Hz | `head_camera_color` |
| `/head_cam/depth/image_rect_raw` | `sensor_msgs/Image` | 640×480 | ~30Hz | `head_camera_depth` |
| `/left_cam/color/image_raw` | `sensor_msgs/Image` | 640×480 | ~30Hz | `left_camera_color` |
| `/left_cam/depth/image_rect_raw` | `sensor_msgs/Image` | 640×480 | ~30Hz | `left_camera_depth` |
| `/right_cam/color/image_raw` | `sensor_msgs/Image` | 640×480 | ~30Hz | `right_camera_color` |
| `/right_cam/depth/image_rect_raw` | `sensor_msgs/Image` | 640×480 | ~30Hz | `right_camera_depth` |

#### ROS 2 Service 接口表

| Service | 类型 | 说明 |
|---------|------|------|
| `/reset_simulation` | `simulation_interfaces/srv/ResetSimulation` | 复位仿真 |
| `/set_simulation_state` | `simulation_interfaces/srv/SetSimulationState` | 设置仿真状态 |
| `/get_entity_state` | `simulation_interfaces/srv/GetEntityState` | 获取实体状态 |

#### ros2_control 接口

```bash
# 查看控制器列表
ros2 control list_controllers

# 预期输出
joint_state_broadcaster[joint_state_broadcaster/JointStateBroadcaster] active
ocs2_arm_controller[ocs2_ros2_controllers/OCS2ArmController] active
```


---

## 7. 启动流程

### 7.1 标准三终端启动流程

#### 终端 A: 启动 Isaac Sim

```bash
cd /path/to/Isaac-Robot-Simulation
export ROS_DOMAIN_ID=73

# 预检（可选）
./scripts/project_control_20260723.sh preflight

# 启动 Isaac Sim（自动加载场景和重建相机）
./scripts/project_control_20260723.sh start-isaac
```

**等待输出**:
```
[OK] RenderProduct created: head/rgb
[OK] RenderProduct created: head/depth
[OK] RenderProduct created: left/rgb
[OK] RenderProduct created: left/depth
[OK] RenderProduct created: right/rgb
[OK] RenderProduct created: right/depth
```

**⚠️ 重要**: 在 Isaac Sim GUI 中手动点击 **▶ Play** 按钮

#### 终端 B: 启动 OCS2 控制器

```bash
cd /path/to/Isaac-Robot-Simulation
export ROS_DOMAIN_ID=73
source /opt/ros/humble/setup.bash
source projects/ros2_ws/install/setup.bash

# 启动 OCS2、ros2_control、RViz2
./scripts/project_control_20260723.sh launch-ocs2
```

**等待控制器激活**:
```bash
ros2 control list_controllers
# 预期: joint_state_broadcaster active
#      ocs2_arm_controller active
```

#### 终端 C: 检查并示教

```bash
cd /path/to/Isaac-Robot-Simulation
export ROS_DOMAIN_ID=73
source /opt/ros/humble/setup.bash
source projects/ros2_ws/install/setup.bash

# 检查运行状态
./scripts/project_control_20260723.sh sim1-check

# 基础示教
./scripts/project_control_20260723.sh sim1-teach

# 完整示教（推荐）
./scripts/project_control_20260723.sh sim1-teach \
  --open-camera-grid \
  --input-mode pygame \
  --linear-speed 0.025 \
  --angular-speed-deg 4.0 \
  --control-rate 50 \
  --record-cameras \
  --camera-depth-save-every 1
```

### 7.2 键盘控制说明

#### 选择控制对象
```
1  - 左臂
2  - 右臂
3  - 底盘
```

#### 左臂/右臂控制（选中后）
```
位置控制:
  W/S  - X 轴前进/后退
  A/D  - Y 轴左/右
  R/F  - Z 轴上/下

姿态控制:
  I/K  - Pitch +/-
  J/L  - Yaw +/-
  U/O  - Roll +/-
```

#### 夹爪控制
```
Space - 切换当前选中手臂的夹爪开/合
V/B   - 左夹爪闭合/打开
N/M   - 右夹爪闭合/打开
```

#### 底盘控制（选中底盘后）
```
↑  - 前进
↓  - 后退
←  - 左转 90° 圆弧
→  - 右转 90° 圆弧
```

#### 系统控制
```
+/-   - 调整步长
P     - 立即发布当前目标
H     - 显示帮助
Z     - 左臂恢复初始姿态
X     - 右臂恢复初始姿态
Q/Esc - 结束并保存轨迹
```

### 7.3 构建说明

#### 宿主机构建

```bash
cd /path/to/Isaac-Robot-Simulation

# 仅构建核心包（推荐）
./scripts/project_control_20260723.sh build

# 构建全部包
./scripts/project_control_20260723.sh build --all
```

**注意事项**:
- 宿主机使用 `build/install/log` 目录
- Docker 使用 `build_docker/install_docker/log_docker` 目录
- **不要混用**两套构建产物

#### Docker 构建

```bash
cd /path/to/Isaac-Robot-Simulation

# 方法1: 使用构建脚本（推荐）
docker run --rm --user "$(id -u):$(id -g)" \
  -e ROS_DISTRO_TARGET=jazzy \
  -e ROS_DOMAIN_ID=73 \
  -v "$(pwd):/workspace" \
  -w /workspace \
  issac_ocs_docker:5.1.0-repro \
  /bin/bash /workspace/scripts/docker_build_ros2_ws_20260730.sh

# 方法2: 进入容器手动构建
docker exec -it demo_vla bash
ocs_build_all  # 别名，构建全部 116 个支持包
```

### 7.4 停止流程

**正确停止顺序**:

1. **终端 C**: 在示教窗口按 `Q` 或 `Esc`，等待数据保存完成
2. **终端 B**: 按 `Ctrl+C` 停止 OCS2 launch
3. **终端 A**: 
   - 在 Isaac Sim 中点击 **■ Stop** 停止 Play
   - 关闭 Isaac Sim 窗口

**⚠️ 注意**:
- 不要在数据保存过程中强制终止
- 不要在 OCS2 运行时关闭 Isaac Sim
- 如需复位，使用 `sim1-reset` 而不是重启

---

## 8. 数据流程

### 8.1 数据采集流程

```
键盘示教
  ↓
实时记录 OCS2 目标、当前位姿、夹爪状态、底盘命令
  ↓
保存 CSV 文件
  ↓
自动清洗（去重、平滑、插值）
  ↓
转换为 LeRobot v3.0 格式
  ↓
可用于训练
```

### 8.2 数据存储结构

```
projects/Trajectory/SIM1/trace_data/
├── raw/                                    # 原始数据
│   ├── manual_ocs2_keyboard_trace_YYYYmmdd_HHMMSS.csv
│   ├── manual_ocs2_keyboard_trace_YYYYmmdd_HHMMSS.diagnostics.csv
│   ├── manual_ocs2_keyboard_trace_YYYYmmdd_HHMMSS.metadata.json
│   ├── manual_ocs2_keyboard_trace_YYYYmmdd_HHMMSS.label.md
│   └── manual_ocs2_keyboard_trace_latest.csv  # 符号链接
│
├── cleaned/                                # 清洗后数据
│   ├── *_cleaned.csv
│   └── *_cleaned.report.md
│
├── lerobot_v30/                           # LeRobot v3.0 格式
│   └── episode_*.npz
│
└── lerobot_v21/                           # LeRobot v2.1（可选）
    └── ...
```

### 8.3 轨迹数据格式

#### CSV 主文件字段

```csv
timestamp,
left_target_x, left_target_y, left_target_z,
left_target_qx, left_target_qy, left_target_qz, left_target_qw,
right_target_x, right_target_y, right_target_z,
right_target_qx, right_target_qy, right_target_qz, right_target_qw,
left_current_x, left_current_y, left_current_z,
left_current_qx, left_current_qy, left_current_qz, left_current_qw,
right_current_x, right_current_y, right_current_z,
right_current_qx, right_current_qy, right_current_qz, right_current_qw,
left_gripper_cmd, right_gripper_cmd,
base_linear_x, base_angular_z
```

#### Metadata JSON

```json
{
  "trace_id": "manual_ocs2_keyboard_trace_20260715_133454",
  "start_time": "2026-07-15T13:34:54",
  "duration_sec": 45.2,
  "sample_count": 2260,
  "control_rate_hz": 50.0,
  "task_label": "pick_and_place",
  "success": true
}
```

### 8.4 相机数据录制

#### 录制命令

```bash
# 示教时自动录制
./scripts/project_control_20260723.sh sim1-teach \
  --record-cameras \
  --camera-depth-save-every 1  # 每帧保存 depth

# 或使用独立录制工具
cd projects/Trajectory/SIM1/tools
python3 camera_recorder.py \
  --output-dir /path/to/output \
  --duration 60 \
  --save-rgb-video \
  --save-depth-npy
```

#### 录制数据结构

```
camera_recording_YYYYmmdd_HHMMSS/
├── summary.json                 # 录制元数据
├── camera_timestamps.csv        # 时间戳对齐
├── videos/
│   ├── head_rgb.mp4
│   ├── left_rgb.mp4
│   └── right_rgb.mp4
└── depth/
    ├── head_depth/
    │   ├── 000000.npy
    │   ├── 000001.npy
    │   └── ...
    ├── left_depth/
    └── right_depth/
```

### 8.5 数据清洗与转换

#### 清洗脚本

```bash
cd projects/Trajectory/SIM1

# 自动清洗（示教结束后自动执行）
# 手动清洗
./trace_cleaning/scripts/process_sim1_episode.py \
  trace_data/raw/manual_ocs2_keyboard_trace_20260715_133454.csv
```

**清洗步骤**:
1. 去除重复时间戳
2. 平滑异常抖动
3. 插值缺失数据点
4. 生成诊断报告

#### LeRobot 转换

```bash
# 转换为 v3.0 格式（默认）
./trace_cleaning/scripts/convert_to_lerobot_v30.py \
  trace_data/cleaned/*_cleaned.csv

# 如需 v2.1 格式
export SIM1_GENERATE_LEROBOT_V21=1
./trace_cleaning/scripts/convert_to_lerobot_v21.py \
  trace_data/cleaned/*_cleaned.csv
```

### 8.6 当前保留数据

**主要轨迹**:
```
projects/Trajectory/SIM1/trace_data/raw/
  manual_ocs2_keyboard_trace_20260715_133454.csv  (原始)
  
projects/Trajectory/SIM1/trace_data/cleaned/
  manual_ocs2_keyboard_trace_20260715_133454_cleaned.csv  (清洗后)
```

**Docker 验收数据**:
```
outputs/docker_acceptance_20260730/
  ocs2_motion_trace.csv  (双臂运动验证)
  
outputs/docker_acceptance_20260730/six_camera_recording/
  summary.json  (六路相机验证)
```

**⚠️ 数据不足**:
- 当前无完整的任务标注数据集
- 无足够的多样化训练样本
- 需要后续系统化采集

---

## 9. Docker 使用

### 9.1 镜像信息

| 属性 | 值 |
|------|-----|
| 镜像名称 | `issac_ocs_docker:5.1.0-repro` |
| 备用标签 | `issac_ocs_docker:latest` |
| 镜像 ID | `7922d3590bde19a51e09c7ccfa0343aefbfce60d4e40a986f4140d9aea3d94d7` |
| 基础镜像 | `nvcr.io/nvidia/isaac-sim:5.1.0@sha256:93b0f99635...` |
| 大小 | ~19 GB |

### 9.2 构建镜像

```bash
cd /path/to/Isaac-Robot-Simulation

# 完整构建（带缓存）
docker build \
  --build-arg ROS_DISTRO_TARGET=jazzy \
  -t issac_ocs_docker:5.1.0-repro \
  -t issac_ocs_docker:latest \
  -f docker/Dockerfile .

# 无缓存构建
docker build --no-cache \
  --build-arg ROS_DISTRO_TARGET=jazzy \
  -t issac_ocs_docker:5.1.0-repro \
  -f docker/Dockerfile .
```

**构建时间**: 约 30-60 分钟（取决于网络和硬件）

### 9.3 启动容器

```bash
cd /path/to/Isaac-Robot-Simulation

# 启动（或替换已有容器）
bash docker/run_isaac_ocs_docker.sh --replace

# 进入容器
docker exec -it demo_vla bash
```

**容器配置**:
- 名称: `demo_vla`
- 网络: host
- IPC: host
- PID: host
- GPU: 全部 (`--gpus all`)
- X11: 已挂载
- 用户: 当前 UID/GID
- 挂载: `项目根目录` → `/workspace`
- shm: 100 GB
- tmpfs: 4 GB (`/tmp`)

### 9.4 容器内操作

#### 环境检查

```bash
# 进入容器
docker exec -it demo_vla bash

# 检查环境
docker_env_check.sh

# 检查 GPU
nvidia-smi

# 检查 ROS
echo $ROS_DISTRO  # jazzy
ros2 pkg list | head
```

#### 构建 ROS 工作区

```bash
# 方法1: 使用别名（推荐）
ocs_build_all

# 方法2: 手动构建
cd /workspace/projects/ros2_ws
source /opt/ros/jazzy/setup.bash
colcon --log-base log_docker build \
  --build-base build_docker \
  --install-base install_docker \
  --symlink-install \
  --cmake-args -DBUILD_TESTING=OFF
```

#### 运行验证

```bash
# Source 环境
source /opt/ros/jazzy/setup.bash
source /workspace/projects/ros2_ws/install_docker/setup.bash

# 检查包
ros2 pkg prefix r1_description
ros2 pkg prefix ocs2_arm_controller

# 检查场景
test -f /workspace/assets/scenes/scene.usd && echo "OK"

# 检查 ROS 工作区
ocs_check
```

#### 容器内别名

容器自动加载以下别名（定义在 `docker/shell_aliases.sh`）:

```bash
ocs_source         # Source ROS 环境
ocs_build_all      # 构建全部包
ocs_build_core     # 构建核心包
ocs_check          # 检查环境
ocs_launch         # 启动 OCS2

sim1_check         # SIM1 检查
sim1_teach         # SIM1 示教
sim1_demo          # SIM1 回放
sim1_reset         # SIM1 复位
sim1_clean         # 数据清洗
sim1_convert       # LeRobot v3.0 转换
sim1_convert_v21   # LeRobot v2.1 转换
```

### 9.5 镜像管理

```bash
# 查看镜像
docker images | grep issac_ocs_docker

# 查看容器
docker ps -a | grep demo_vla

# 停止容器
docker stop demo_vla

# 删除容器
docker rm demo_vla

# 删除镜像
docker rmi issac_ocs_docker:5.1.0-repro

# 清理悬空镜像
docker image prune -f

# 查看镜像详情
docker image inspect issac_ocs_docker:5.1.0-repro
```

### 9.6 容器内环境变量

```bash
WORKSPACE=/workspace
ROS2_WS=/workspace/projects/ros2_ws
ROS2_INSTALL_DIR=/workspace/projects/ros2_ws/install_docker
TRAJECTORY_DIR=/workspace/projects/Trajectory
SIM1_DIR=/workspace/projects/Trajectory/SIM1
ROS2_LOG_DIR=/workspace/data/ros2_log
ISAAC_USD_PATH=/workspace/assets/scenes/scene.usd
OUTPUT_DIR=/workspace/outputs

ROS_DISTRO=jazzy
ROS_DISTRO_TARGET=jazzy
ROS_DOMAIN_ID=0
RMW_IMPLEMENTATION=rmw_fastrtps_cpp
GZ_VERSION=harmonic
```


---

## 10. 物理仿真参数

### 10.1 参数搜索概述

项目包含系统化的物理参数搜索流程，用于确定机器人在仿真中的最优放置参数和物理属性。

**搜索目录**: `物理仿真与参数获取/`

### 10.2 搜索维度

| 维度 | 参数 | 搜索范围 | 说明 |
|------|------|----------|------|
| Y 位置 | `y` | 变化范围 | 机器人相对传送带的 Y 轴位置 |
| 偏航角 | `yaw` | 旋转角度 | 机器人基座偏航角 |
| 俯仰角 | `pitch` | 倾斜角度 | 机器人基座俯仰角 |
| 质量 | `mass` | kg | 机器人及负载质量 |
| 摩擦系数 | `friction` | 0.0-1.0 | 接触面摩擦系数 |

### 10.3 搜索脚本结构

```
物理仿真与参数获取/
├── SEARCH8/                    # 最新版本搜索脚本
│   ├── step1_search_y.py       # 步骤1: 搜索 Y 位置
│   ├── step2_search_yaw.py     # 步骤2: 搜索偏航角
│   ├── step3_search_pitch.py   # 步骤3: 搜索俯仰角
│   ├── step4_search_mass.py    # 步骤4: 搜索质量
│   └── step5_search_friction.py # 步骤5: 搜索摩擦系数
│
├── result/                     # 搜索结果
│   ├── final_release_space.json     # 最终参数空间
│   ├── final_release_space.md       # 参数说明文档
│   ├── safe_pose.json               # 安全姿态集合
│   ├── safe_pose_y.json             # Y 维度结果
│   ├── safe_pose_yaw.json           # Yaw 维度结果
│   ├── safe_pose_pitch.json         # Pitch 维度结果
│   ├── safe_pose_mass.json          # Mass 维度结果
│   ├── y_analysis.py                # Y 维度分析脚本
│   ├── yaw_analysis.py              # Yaw 维度分析脚本
│   ├── pitch_analysis.py            # Pitch 维度分析脚本
│   ├── mass_analysis.py             # Mass 维度分析脚本
│   └── friction_analysis.py         # Friction 维度分析脚本
│
└── pos/                        # 场景构建脚本
    ├── scene_build.py          # 场景构建
    ├── align_to_ground.py      # 地面对齐
    ├── check_Friction.py       # 摩擦检查
    └── ...
```

### 10.4 搜索流程

```
Step 1: 搜索 Y 位置
  ↓
Step 2: 在最优 Y 下搜索 Yaw
  ↓
Step 3: 在最优 Y, Yaw 下搜索 Pitch
  ↓
Step 4: 在最优姿态下搜索 Mass
  ↓
Step 5: 在最优配置下搜索 Friction
  ↓
生成最终参数空间
```

### 10.5 最终参数文件

**文件**: `物理仿真与参数获取/result/final_release_space.json`

包含所有维度的最优参数范围，用于：
- 场景初始化
- 随机化配置
- 域随机化（Domain Randomization）

### 10.6 使用方法

```bash
cd /path/to/Isaac-Robot-Simulation/物理仿真与参数获取

# 查看最终参数
cat result/final_release_space.json

# 查看参数说明
cat result/final_release_space.md

# 运行分析脚本
cd result
python3 y_analysis.py
python3 yaw_analysis.py
```

---

## 11. 常见问题

### 11.1 启动问题

#### Q1: Isaac Sim 打开后是空场景

**原因**: 未正确加载场景文件

**解决**:
```bash
# 确保使用统一入口
./scripts/project_control_20260723.sh start-isaac

# 或显式指定场景
./scripts/project_control_20260723.sh start-isaac \
  assets/scenes/scene.usd
```

#### Q2: 找不到 ROS 包

**原因**: 未 source 正确的 ROS 环境

**解决**:
```bash
# 宿主机
source /opt/ros/humble/setup.bash
source projects/ros2_ws/install/setup.bash

# Docker
source /opt/ros/jazzy/setup.bash
source /workspace/projects/ros2_ws/install_docker/setup.bash
```

#### Q3: 控制器未激活

**检查**:
```bash
ros2 control list_controllers
```

**解决**:
```bash
# 确保 Isaac Sim 已按 Play
# 重启 OCS2 launch
./scripts/project_control_20260723.sh launch-ocs2
```

### 11.2 相机问题

#### Q4: 相机 topic 无数据

**检查**:
```bash
ros2 topic list | grep cam
ros2 topic hz /head_cam/color/image_raw
```

**原因**:
1. Isaac Sim 未按 Play
2. RenderProduct 未创建
3. Camera_Publish_Graph 未激活

**解决**:
```bash
# 确保按 Play
# 查看重建日志
cat outputs/camera_session_rebuild/latest.json

# 如需手动重建
/isaac-sim/python.sh scripts/rebuild_six_camera_render_products_20260730.py
```

#### Q5: 相机监看窗口显示 "no publisher"

**原因**: 未启动 Isaac Sim 或未按 Play

**解决**:
1. 确保 Isaac Sim 已启动并加载场景
2. 确保已按 Play
3. 确认相机 topic 有数据：`ros2 topic list | grep cam`

### 11.3 控制问题

#### Q6: 机械臂不响应命令

**检查**:
```bash
# 检查 FSM 状态
ros2 topic echo --once /fsm_command

# 检查关节命令
ros2 topic hz /arm_joint_cmd
```

**解决**:
```bash
# 激活 OCS2
ros2 topic pub --once /fsm_command std_msgs/msg/Int32 "{data: 3}"

# 检查控制器
ros2 control list_controllers
```

#### Q7: 夹爪不动作

**检查**:
```bash
ros2 topic echo /left_gripper_controller/commands
ros2 topic echo /right_gripper_controller/commands
```

**解决**:
- 确认场景使用 PGIA v4.5 脚本
- 检查 Gripper_Control_Graph 连线
- 确认命令值：`> 0` 打开，`<= 0` 闭合

#### Q8: 示教出现误差告警

**现象**: "gate warning" 或 "watchdog warning"

**原因**: 目标与当前位姿误差过大

**解决**:
1. 松开移动键，等待收敛
2. 按 `Z`（左臂）或 `X`（右臂）恢复初始姿态
3. 降低移动速度：`--linear-speed 0.01`

### 11.4 Docker 问题

#### Q9: Docker 构建失败

**检查**:
```bash
# 检查磁盘空间
df -h

# 检查 Docker 空间
docker system df
```

**解决**:
```bash
# 清理悬空镜像
docker image prune -f

# 清理停止的容器
docker container prune -f

# 如需完全重建
docker build --no-cache -f docker/Dockerfile .
```

#### Q10: 容器内找不到文件

**原因**: 挂载路径问题

**检查**:
```bash
# 进入容器
docker exec -it demo_vla bash

# 检查挂载
ls -la /workspace
echo $WORKSPACE
```

**解决**: 确保启动时正确挂载：
```bash
bash docker/run_isaac_ocs_docker.sh --replace
```

### 11.5 编译问题

#### Q11: colcon 构建失败

**常见原因**:
1. 混用宿主机和 Docker 构建产物
2. CMake 缓存污染
3. 依赖缺失

**解决**:
```bash
# 清理构建缓存
cd projects/ros2_ws
rm -rf build install log

# 重新构建
./scripts/project_control_20260723.sh build

# 或在 Docker 中
rm -rf build_docker install_docker log_docker
ocs_build_all
```

#### Q12: 路径指向错误的 ROS 发行版

**现象**: 错误提示包含 `/opt/ros/humble` 但在 Jazzy 容器中

**原因**: CMake 缓存污染

**解决**:
```bash
# Docker 中
cd /workspace/projects/ros2_ws
rm -rf build_docker
colcon --log-base log_docker build \
  --build-base build_docker \
  --install-base install_docker \
  --cmake-clean-cache  # 清理缓存
```

### 11.6 数据问题

#### Q13: 轨迹文件找不到

**检查**:
```bash
ls -la projects/Trajectory/SIM1/trace_data/raw/
ls -la projects/Trajectory/SIM1/trace_data/cleaned/
```

**解决**:
- 确保示教正常结束（按 `Q` 或 `Esc`）
- 检查 `ROS2_LOG_DIR` 环境变量
- 查看最新轨迹：`manual_ocs2_keyboard_trace_latest.csv`

#### Q14: LeRobot 数据转换失败

**检查**:
```bash
cd projects/Trajectory/SIM1/trace_cleaning
python3 scripts/convert_to_lerobot_v30.py --help
```

**解决**:
- 确保输入是清洗后的 CSV 文件
- 检查 Python 依赖：`numpy`, `pandas`

---

## 12. 重要备注

### 12.1 ⚠️ 关键注意事项

1. **不要删除或移动 `assets/scenes/615scene_20260723/` 目录**
   - 这是场景的依赖层，包含模型和资源
   - 删除会导致场景加载失败

2. **不要混用宿主机和 Docker 的构建产物**
   - 宿主机：`build/`, `install/`, `log/`
   - Docker：`build_docker/`, `install_docker/`, `log_docker/`

3. **不要在未 source ROS 环境时直接运行 Isaac Sim**
   - 使用统一入口：`./scripts/project_control_20260723.sh start-isaac`
   - 入口会清理环境变量冲突

4. **不要将会话级 RenderProduct 路径保存到根 USD**
   - RenderProduct 是临时对象，每次启动自动重建
   - 当前方案已验证正确

5. **不要在 OCS2 运行时使用 `direct-demo`**
   - 两者都会发布 `/arm_joint_cmd`，会冲突
   - `direct-demo` 用于 LeRobot 直接回放，不经过 OCS2

6. **不要修改 `project_manifest.yaml` 中的路径定义**
   - 这是项目规范配置
   - 所有脚本都依赖这些定义

7. **启动 Isaac Sim 后必须手动按 Play**
   - 自动 Play 仅用于测试
   - 生产环境需手动确认

8. **修改场景前务必备份**
   ```bash
   cp assets/scenes/scene.usd assets/scenes/scene.usd.bak_$(date +%Y%m%d_%H%M%S)
   sha256sum assets/scenes/scene.usd > scene.usd.sha256
   ```

### 12.2 🔒 权限与安全

- Docker 容器使用 `--privileged` 和 host 网络
- 仅在可信环境中运行
- 不要暴露容器端口到公网

### 12.3 📊 性能建议

- **GPU 内存**: 建议 ≥ 16 GB
- **系统内存**: 建议 ≥ 32 GB
- **磁盘空间**: 
  - 项目: ~10 GB
  - Docker 镜像: ~19 GB
  - 总计建议: ≥ 50 GB

### 12.4 🗂️ 备份策略

**重要文件备份**:
```bash
# 场景文件
assets/scenes/scene.usd

# 配置文件
project_manifest.yaml

# 核心脚本
scripts/project_control_20260723.sh
projects/Trajectory/SIM1/game_control.sh

# 数据
projects/Trajectory/SIM1/trace_data/
outputs/
```

**定期备份命令**:
```bash
tar -czf isaac_ocs_backup_$(date +%Y%m%d).tar.gz \
  --exclude='build*' \
  --exclude='install*' \
  --exclude='log*' \
  --exclude='.git' \
  ./
```

---

## 13. 后续工作建议

### 13.1 优先级 P0（紧急，必须完成）

1. **采集完整的任务标注数据集**
   - 目标：至少 100 条成功示教轨迹
   - 包含六路 RGB-D 完整录制
   - 明确任务标签（pick, place, push, etc.）

2. **实现任务成功自动判定**
   - 基于物体位置
   - 基于关键点检测
   - 保存成功/失败标签

### 13.2 优先级 P1（重要，短期完成）

3. **场景自动随机化**
   - 物体位置随机化
   - 光照随机化
   - 纹理随机化
   - 使用 `final_release_space.json` 参数

4. **批量数据采集管道**
   - 自动复位
   - 自动示教（或远程操作）
   - 自动判定
   - 批量保存

5. **数据增强与验证**
   - 轨迹平滑算法优化
   - 数据完整性检查
   - 视觉数据质量验证

### 13.3 优先级 P2（重要，中期完成）

6. **模型训练管线搭建**
   - BC (Behavior Cloning) 基线
   - ACT (Action Chunking Transformer)
   - Diffusion Policy
   - 训练/验证/测试分割

7. **模型评估系统**
   - 仿真成功率
   - 轨迹质量评估
   - 泛化能力测试

8. **可视化与监控**
   - TensorBoard 集成
   - 实时训练监控
   - 模型性能 Dashboard

### 13.4 优先级 P3（可选，长期目标）

9. **VLA (Vision-Language-Action) 集成**
   - 语言指令接口
   - 多模态输入
   - 预训练模型微调

10. **强化学习环境**
    - Gym/Gymnasium 接口
    - 奖励函数设计
    - PPO/SAC 算法集成

11. **Sim2Real 迁移**
    - 真机硬件对接
    - 域适应算法
    - 安全保护机制

12. **分布式训练**
    - 多 GPU 训练
    - 多机分布式
    - 云端训练支持

### 13.5 技术债务

- 清理旧的兼容符号链接（`/home/gtk/Trajectory`, etc.）
- 完善单元测试覆盖
- 补充 API 文档
- 优化构建时间
- 减小 Docker 镜像体积

---

## 14. 参考文档

### 14.1 项目内文档

| 文档 | 路径 | 说明 |
|------|------|------|
| 中文说明 | `README_CN.md` | 项目概述 |
| 英文说明 | `README.md` | Project overview |
| 操作指南 | `GAME_GUIDE.md` | 详细操作说明 |
| 启动指南 | `docs/startup_guide_20260723_zh.md` | 启动教程 |
| Docker 文档 | `docker/README_DOCKER.md` | Docker 使用说明 |
| 复现指南 | `REPRODUCE.md` | 冷启动复现 |
| 原交接文档 | `docs/PROJECT_HANDOVER_20260730_zh.md` | 2026-07-30 版本 |
| 机器人资源策略 | `docs/ROBOT_RESOURCE_POLICY.md` | 资源管理 |
| 轨迹数据格式 | `projects/Trajectory/SIM1/TRACE_DATA_FORMAT.md` | 数据格式说明 |
| SIM1 概览 | `projects/Trajectory/SIM1/PROJECT_OVERVIEW.md` | SIM1 项目说明 |

### 14.2 外部文档（本地）

| 文档 | 路径 |
|------|------|
| Isaac Sim 5.1.0 官方文档 | `/home/gtk/ai_docs/docs.isaacsim.omniverse.nvidia.com/5.1.0/` |
| ROS 2 Rolling 文档 | `/home/gtk/ai_docs/docs.ros.org/en/rolling/` |

### 14.3 在线资源

- **Isaac Sim**: https://docs.omniverse.nvidia.com/isaacsim/latest/
- **ROS 2**: https://docs.ros.org/
- **OCS2**: https://leggedrobotics.github.io/ocs2/
- **LeRobot**: https://github.com/huggingface/lerobot
- **Pinocchio**: https://github.com/stack-of-tasks/pinocchio

### 14.4 关键配置文件

| 文件 | 说明 |
|------|------|
| `project_manifest.yaml` | 项目配置清单 |
| `dependencies/repositories.lock.yaml` | 外部仓库版本锁定 |
| `dependencies/docker-python-requirements.lock` | Python 依赖锁定 |
| `docker/Dockerfile` | Docker 镜像构建 |
| `assets/scenes/scene.usd` | 主场景文件 |

---

## 附录 A: 快速命令参考

### 宿主机常用命令

```bash
# 环境准备
cd /path/to/Isaac-Robot-Simulation
export ROS_DOMAIN_ID=73
source /opt/ros/humble/setup.bash
source projects/ros2_ws/install/setup.bash

# 启动
./scripts/project_control_20260723.sh start-isaac    # 终端 A
./scripts/project_control_20260723.sh launch-ocs2    # 终端 B
./scripts/project_control_20260723.sh sim1-teach     # 终端 C

# 检查
ros2 topic list
ros2 control list_controllers
ros2 topic hz /head_cam/color/image_raw

# 回放
./scripts/project_control_20260723.sh sim1-demo
```

### Docker 常用命令

```bash
# 启动容器
cd /path/to/Isaac-Robot-Simulation
bash docker/run_isaac_ocs_docker.sh --replace
docker exec -it demo_vla bash

# 容器内
ocs_source
ocs_build_all
ocs_check
sim1_check
sim1_teach
```

### 数据处理命令

```bash
cd projects/Trajectory/SIM1

# 清洗
./trace_cleaning/scripts/process_sim1_episode.py \
  trace_data/raw/manual_ocs2_keyboard_trace_20260715_133454.csv

# 转换
./trace_cleaning/scripts/convert_to_lerobot_v30.py \
  trace_data/cleaned/*_cleaned.csv
```

---

## 附录 B: 故障排查检查清单

### 启动前检查

- [ ] GPU 驱动正常：`nvidia-smi`
- [ ] Isaac Sim 路径正确：`ls $ISAAC_SIM_ROOT`
- [ ] ROS 环境已 source
- [ ] 项目布局验证通过：`./scripts/verify_project_layout.sh`

### 启动时检查

- [ ] Isaac Sim 已打开场景：`assets/scenes/scene.usd`
- [ ] 六个 RenderProduct 创建成功
- [ ] Isaac Sim 已按 **Play**
- [ ] OCS2 控制器已激活：`ros2 control list_controllers`

### 运行时检查

- [ ] 相机 topic 有数据：`ros2 topic hz /head_cam/color/image_raw`
- [ ] 关节状态正常：`ros2 topic hz /joint_states`
- [ ] 双臂位姿有反馈：`ros2 topic hz /left_current_pose`
- [ ] FSM 状态正确：`ros2 topic echo --once /fsm_command`

---

## 附录 C: 联系信息与资源

如遇到无法解决的技术问题，建议：
1. 查阅本文档 "常见问题" 章节

2. 查看 `docs/` 目录下的详细文档

3. 检查 `reports/` 目录的历史报告

   
